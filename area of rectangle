#include <stdio.h>

int main()
{
int length,width,area;
printf("enter the value of length");
scanf("%d",&length);
printf("enter the value of width");
scanf("%d",&width);
area=length*width;
printf("the area of %d",area);
}
